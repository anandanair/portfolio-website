export class EducationErrorModel {
  constructor(schoolName, field, startDate, endDate) {
    this.schoolName = schoolName;
    this.field = field;
    this.startDate = startDate;
    this.endDate = endDate;
  }
}
